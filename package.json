{
  "name": "ownerrep-cost-boq",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "OwnerRep-ERP — módulo BOQ (Fase 1, estimador). Motor de cálculo.",
  "scripts": {
    "dev": "vite",
    "api": "tsx src/server.ts",
    "start": "concurrently -k -n API,WEB -c blue,green \"npm:api\" \"npm:dev\"",
    "build": "vite build",
    "test": "vitest run",
    "test:watch": "vitest",
    "typecheck": "tsc --noEmit"
  },
  "devDependencies": {
    "@types/better-sqlite3": "^7.6.13",
    "concurrently": "^10.0.3",
    "tsx": "^4.22.4",
    "typescript": "^5.6.0",
    "vite": "^5.4.21",
    "vitest": "^2.1.0"
  },
  "dependencies": {
    "better-sqlite3": "^12.10.0",
    "drizzle-orm": "^0.45.2",
    "exceljs": "^4.4.0"
  }
}
