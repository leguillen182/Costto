# Decisiones de arquitectura (ADR) — puntero

Las ADR y el roadmap **canónicos NO viven en este repo**: están en el workspace de Obsidian,
en `Proyectos/OwnerRep-ERP/` (gestión viva del proyecto).

Referencias rápidas a decisiones citadas en el código:

- **ADR-011** — Redondeo monetario: `roundingDecimals` por BOQ, default 2.
- **ADR-013** — Precisión monetaria en float (**diferida**): los redondeos actuales son una
  decisión consciente; no "arreglarlos" sin revisar el ADR.

Si buscas "el roadmap" en el repo, no está aquí: ver Obsidian (y `BACKLOG.md` para
mejoras pequeñas diferidas).
